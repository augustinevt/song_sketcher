{
  "name": "component-tester",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "chord-generator": "^1.0.2",
    "chord-sketch-ui": "^1.0.2",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-scripts": "3.1.2",
    "styled-components": "^4.4.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "man:publish": "aws s3 sync ./build s3://chord-sketch --acl public-read --cache-control \"max-age=86400\""
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
